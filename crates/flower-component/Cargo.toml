[package]
name = "flower-component"
version = "0.2.0"
edition = "2024"

[lib]
crate-type = ["cdylib"]

[dependencies]
flower-plan = { path = "../flower-plan" }
flower-compiler = { path = "../flower-compiler" }
flower-kernel = { path = "../flower-kernel" }
wit-bindgen.workspace = true
